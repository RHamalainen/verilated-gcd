name: build top
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: |
                sudo apt-get update
                sudo apt-get install iverilog
                iverilog -o top top.v
                vvp top
